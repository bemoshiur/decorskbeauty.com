// Geist Sans (UI) + Geist Mono (numerals/prices), self-hosted via the `geist` package's next/font
// integration — each exposes `.variable` for the CSS custom properties used in the layout.
export { GeistSans } from 'geist/font/sans'
export { GeistMono } from 'geist/font/mono'
